#include "./number.hpp"
#include <stdio.h>
namespace js{
    /*Console.log:
    template <typename ...Args>
    void print(Args... args){
        Array<Object> arr = {};
        (arr.push(args), ...);
        printf(arr.join().std() + '\n');
    }
    */

    class null {

    }

    class undefined {

    }

    class NaN NaN;
}